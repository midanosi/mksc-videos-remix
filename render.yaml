databases:
  - name: mkscvids
    plan: free

services:
  - type: web
    name: mkscvids-web
    env: node
    plan: free
    buildCommand: pnpm i --frozen-lockfile && pnpm run build && pnpm prune --production
    startCommand: npx prisma migrate deploy && pnpm run start
    healthCheckPath: /healthcheck
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mkscvids
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: NODE_ENV #required so NODE_ENV is set in build env
        value: production
